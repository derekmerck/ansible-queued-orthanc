galaxy_info:
  author: Derek Merck
  description: Configure and run a queued orthanc arrangement where the primary archive is fed by an anonymization and compression queue.

  role_name: queued-orthanc

  license: MIT

  min_ansible_version: 2.4

  galaxy_tags:
  - docker
  - dicom

dependencies: []
#  - role: derekmerck.diana-docker
#    when: (qorth_queue.diana_routing_dir is defined) and (qorth_queue.diana_routing_dir != None)